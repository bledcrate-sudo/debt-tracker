name: Build unsigned iOS IPA

on:
  workflow_dispatch:
    inputs:
      server_url:
        description: "Deployed Next.js URL the app will load"
        required: true
        default: "https://your-debt-tracker.vercel.app"
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 45
    env:
      CAP_SERVER_URL: ${{ github.event.inputs.server_url || 'https://debt-tracker-rho-eight.vercel.app' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Add iOS platform
        run: |
          npx cap add ios
          npx cap sync ios
          echo "--- ios tree ---"
          find ios -maxdepth 4 -type f | head -50

      # cap add ios scaffolds Capacitor's stock AppIcon, so the real icons have
      # to be generated after it exists and before the archive is built.
      - name: Generate app icon and splash
        run: |
          set -eo pipefail
          npx capacitor-assets generate --ios
          echo "--- generated icons ---"
          ls -1 ios/App/App/Assets.xcassets/AppIcon.appiconset | head -20

      - name: Resolve SPM packages
        working-directory: ios/App
        run: |
          ls -la
          xcodebuild -resolvePackageDependencies -project App.xcodeproj -scheme App

      - name: Install CocoaPods
        working-directory: ios/App
        run: pod install --repo-update

      - name: Build archive (no signing)
        working-directory: ios/App
        shell: bash
        run: |
          set -eo pipefail
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -archivePath "$PWD/build/App.xcarchive" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO \
            archive
          echo "--- archive contents ---"
          find build/App.xcarchive -maxdepth 5 -type d

      - name: Package unsigned IPA
        working-directory: ios/App
        shell: bash
        run: |
          set -eo pipefail
          APP_PATH=$(find build/App.xcarchive/Products/Applications -maxdepth 1 -name "*.app" -type d | head -1)
          echo "Using app: $APP_PATH"
          mkdir -p build/Payload
          cp -R "$APP_PATH" build/Payload/
          cd build && zip -qr DebtTracker-unsigned.ipa Payload
          ls -lh DebtTracker-unsigned.ipa

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: DebtTracker-unsigned-ipa
          path: ios/App/build/DebtTracker-unsigned.ipa
          retention-days: 30
