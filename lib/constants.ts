// Shared by server importers and client UI (no server-only imports here).

// Note stamped on entries created by bank sync; the dashboard hides it on
// rows (the date chip already marks them) to keep tables compact.
export const IMPORT_NOTE = "Synced from bank";
